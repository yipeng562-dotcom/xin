
## 2026-07-08 部分代码基线备份

- 备份位置：D:\xin\backup\baseline_2026-07-08_partial
- 已备份：启动窗口程序集、集_登录
- 说明：这是部分基线，不是完整项目基线；仍缺集_任务、公共封装、结构/全局变量、项目文件结构。

## 2026-07-08 补充任务代码到部分基线

- 备份位置：D:\xin\backup\baseline_2026-07-08_partial
- 已补充：03_集_任务_源码.txt
- 来源：C:\Users\Administrator\Desktop\新建 文本文档 (3).txt
- 说明：当前部分基线已包含启动窗口程序集、集_登录、集_任务；仍不是完整项目基线，后续如果涉及公共封装、结构/全局变量、项目文件结构，仍必须让用户提供当前源码后再改。

## 2026-07-08 补充项目文件结构文档

- 新增文档：D:\xin\docs\项目文件结构.md
- 更新规则：D:\xin\AGENTS.md
- 说明：记录当前 `D:\xin` 根目录、docs、backup、downloads 的用途和查找顺序；本次没有修改业务代码。

## 2026-07-08 强化代码读取与备份规则

- 更新规则：D:\xin\AGENTS.md
- 说明：开工自检新增硬规则：涉及代码判断、修改、流程排查时，不能只查快照入口，必须读取对应源码备份；如果用户说明当前代码已有变动，必须同步更新对应源码备份和代码快照。
- 本次没有修改业务代码。

## 2026-07-08 补充用户贴代码校验规则

- 更新规则：D:\xin\AGENTS.md
- 说明：用户贴出任务代码后，必须先检查代码是否正确；不正确先提醒修改，不同步备份和快照；确认正确后再同步更新任务源码备份和任务代码快照。
- 本次没有修改业务代码。

## 2026-07-08 补充上下文恢复强制规则

- 更新规则：D:\xin\AGENTS.md
- 说明：新会话、上下文压缩、无法确认项目状态或丢失上下文时，必须重新读取 AGENTS.md、docs 项目文档和当前相关源码，重新完成强制自检后才能继续开发。
- 本次没有修改业务代码。

## 2026-07-08 同步脚本任务类型结构

- 更新备份：D:\xin\backup\baseline_2026-07-08_partial\01_启动窗口程序集_源码.txt
- 更新快照：D:\xin\docs\01_启动窗口代码快照.txt
- 说明：用户确认 `脚本任务类型` 增加 `是否已处理回想, 逻辑型`；该成员属于 `脚本程序[i]`，适合多开独立记录回想处理状态。
- 本次没有修改任务程序集代码。

# 2026-07-08 任务代码同步：回想标记与技能教学

- 模块：集_任务
- 同步原因：用户确认已改完 `处理主线准备任务`、`处理技能教学` 两段，需要同步备份和快照。
- 备份文件：`D:\xin\backup\baseline_2026-07-08_partial\03_集_任务_源码.txt`
- 快照文件：`D:\xin\docs\03_任务代码快照.txt`
- 变更要点：
  - `处理主线准备任务` 增加 `脚本程序[i].是否已处理回想` 判断，回想只作为首次技能教学入口。
  - `处理主线准备任务` 调用 `处理技能教学(i)` 成功后才标记已处理回想。
  - `处理技能教学` 使用 `飘絮|燕回风` ret 分流。
  - 飘絮分支按 `1/2` 后进入 `战斗中`。
  - 燕回风分支循环按 `A` 后进入 `战斗中`。
  - 补充确认：`处理技能教学` 的 `ret` 局部变量必须是 `整数型`，不是 `整型`。

## 2026-07-08 处理技能教学识别句同步

- 时间：2026-07-08 12:26
- 模块：集_任务
- 修改原因：用户要求将 `处理技能教学` 中原技能教学识别句替换为只识别 `飘絮`。
- 修改内容：更新 `处理技能教学` 的 `FindStrFast` 识别范围、关键词和颜色参数。
- 更新的 current_src：`D:\xin\current_src\03_集_任务_当前源码.txt`
- 更新的 docs 快照：`D:\xin\docs\03_任务代码快照.txt`
- stable_ 备份位置：`D:\xin\backup\stable_任务模块_20260708_1226`
- 是否改动 baseline：否
- 是否测试：未运行易语言/游戏内测试，仅完成文件同步检查。
- 风险：`燕回风` 分支保留但当前识别句只命中 `飘絮`。

## 2026-07-08 18:40 飘絮教学后续状态同步

- 时间：2026-07-08 18:40
- 模块：集_任务
- 修改原因：用户确认已将飘絮技能教学完成后的状态从“战斗中”改为“准备任务”。
- 修改内容：同步 处理技能教学 中飘絮分支后续状态，确保后续回准备任务点击主线下划线。
- 更新的 current_src：D:\xin\current_src\03_集_任务_当前源码.txt
- 更新的 docs 快照：D:\xin\docs\03_任务代码快照.txt
- stable_ 备份位置：$stable
- 是否改动 baseline：否
- 是否测试：未运行易语言/游戏内测试，仅同步用户确认修改。
- 风险：点下划线后出现战斗目标时，观察任务结果当前还未直接分诊战斗目标，需要后续补充战斗目标检测。
## 2026-07-08 18:59 战斗目标临时移除高大胜

- 时间：2026-07-08 18:59
- 模块：集_任务
- 修改原因：用户确认已将战斗目标中的高大胜先删掉，等待后续提供高大胜颜色后再单独处理。
- 修改内容：处理主线战斗中 的战斗目标识别从 家丁|打手|高大胜 改为 家丁|打手，颜色从 d74f4b-281F1B|f0db69-606060 改为 d74f4b-281F1B。
- 更新的 current_src：D:\xin\current_src\03_集_任务_当前源码.txt
- 更新的 docs 快照：D:\xin\docs\03_任务代码快照.txt
- stable_ 备份位置：$stable
- 是否改动 baseline：否
- 是否测试：未运行易语言/游戏内测试，仅同步用户确认修改。
- 风险：高大胜暂时不会作为战斗目标识别，需等用户提供颜色后单独新增找字。
## 2026-07-08 19:02 观察任务结果增加战斗分诊

- 时间：2026-07-08 19:02
- 模块：集_任务
- 修改原因：用户确认已修改 处理主线观察任务结果，点下划线后出现家丁/打手时应切换到战斗中。
- 修改内容：在 处理主线观察任务结果 中，处理交互F 判断结束之后、结果 ＝ 寻路判断 (i) 之前，增加 家丁|打手 检测；命中后切换任务状态为“战斗中”并跳出观察循环。
- 更新的 current_src：D:\xin\current_src\03_集_任务_当前源码.txt
- 更新的 docs 快照：D:\xin\docs\03_任务代码快照.txt
- stable_ 备份位置：$stable
- 是否改动 baseline：否
- 是否测试：未运行易语言/游戏内测试，仅同步用户确认修改。
- 风险：高大胜暂未加入观察分诊，等待用户提供颜色后再单独处理。
## 2026-07-08 21:08:09 任务模块：同步右侧任务目标分诊

时间：2026-07-08 21:08:09
模块：集_任务
修改内容：新增 处理右侧任务目标；在 处理主线观察任务结果 中于 处理交互F 之后、头顶 家丁|打手 检测之前调用右侧任务分诊。右侧任务使用字库1、范围 1010,441,1272,535、颜色 8D5E16-2F1B12、相似度 0.8，识别 高府家丁|高大胜|黑衣人|高衙内|林霁|追命。
Git commit：e4e0a30；28a471d（最终修正插入位置）
是否已 push GitHub：是
更新的 current_src：D:\xin\current_src\03_集_任务_当前源码.txt
更新的 docs 快照：D:\xin\docs\03_任务代码快照.txt
是否创建 backup：是
stable_ 备份位置：D:\xin\backup\stable_任务模块_20260708_2107
自动清理旧备份：D:\xin\backup\stable_任务模块_20260708_1226；D:\xin\backup\stable_任务模块_20260708_1840
是否改动 baseline：否
是否测试：未运行游戏内测试，已完成文件同步与 Git 推送。
风险：高大胜头顶颜色仍未接入战斗目标识别；当前只通过右侧任务文字切入战斗阶段。
